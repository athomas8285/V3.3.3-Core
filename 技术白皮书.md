# V3.3.3-Core 项目技术白皮书

**版本**: Rev1.18 | **最后更新**: 2026-06-23 | **适用环境**: Python 3.14 + SQLite 3 + Flask

---

## 第一章：系统整体架构

### 1.1 技术栈清单

| 层 | 技术选型 | 版本 | 说明 |
|---|---|---|---|
| **后端框架** | Flask | 3.x | 轻量级 WSGI 应用，单进程运行 |
| **Python 运行时** | CPython | 3.14 | 当前 Windows 环境版本 |
| **数据库** | SQLite 3 (WAL模式) | 内置 | 单文件 framework.db，无需单独部署 |
| **前端** | 纯 HTML + CSS + Vanilla JS | — | 无框架，服务端渲染数据注入 |
| **爬虫/API 客户端** | Playwright | async | 无界面浏览器，用于 SofaScore 数据采集 |
| **HTTP 客户端** | requests | 标准库 | 用于 竞彩网 API 直连 |
| **数值计算** | NumPy | 最新 | 蒙特卡洛模拟、泊松分布 |
| **任务编排** | subprocess + 文件监控 | — | 各算法模块通过 JSON 文件解耦 |

### 1.2 目录结构树

`
D:\V3.3.3-Core\
├── app.py                  # Flask 后端入口 (API 路由、页面渲染, 5020端口)
├── dashboard.py            # 管理后台 (只读数据面板, 5021端口)
├── run_all.py              # 全流程分析编排 (λ→MC→DDI→评分→评级)
├── auto_pipeline.py        # 一键全流程入口
│
├── config.py               # 全局配置 (联赛参数、世界杯参数、风控阈值)
├── database.py             # SQLite ORM (建表、CRUD)
├── lambda_calc.py          # λ 物理参数计算 + 世界杯联合会汇率
├── monte_carlo.py          # 泊松分布蒙特卡洛模拟引擎
├── ddi.py                  # DDI 偏差校准 + 赔率动态修正
├── fit_score.py            # 贴合度评分 (7维度评估)
├── rating.py               # 推荐评级 (S/A/B/C + 方向判定)
├── validate.py             # 交叉校验 + 世界杯场景校验
├── save_history.py         # 历史数据归档 (DB + CSV)
│
├── plan_engine.py          # 精选计划单引擎 (API版，单关/串关推荐)
├── gen_plan.py             # 精选计划单独立生成器 (输出 plan_data.json)
├── gen_plan_html.py        # 从 plan_data.json 渲染精美 HTML 页面
├── run_plans_by_day.py     # 按比赛日批量生成历史计划单
├── enrich_odds.py          # 竞彩赔率注入 (raw_jczq → match_info/locked_data)
├── fetch_jczq.py           # 竞彩网数据采集 (5彩池: had/hhad/ttg/crs/hafu)
├── fetch_results.py        # 500.com 赛果补录 (比分+半全场)
├── fetch_form.py           # SofaScore 近期战绩采集
├── build_wc_data.py        # 世界杯赛程数据构建
├── squad_power.py          # 赛前实力预测 (FIFA排名+身价)
├── live_ddi.py             # 临场SP重校引擎
├── backtest.py             # 回测模块
├── review.py               # 复盘模块 (4层诊断: 数据/物理/因子/市场)
├── parser.py               # 原始文本解析
├── smart_factors.py        # AI因子参数生成器
├── gen_batch.py            # 批量数据生成
├── factors.py              # 因子计算
├── pipeline.py             # 管道监控 (文件监听模式)
│
├── templates/
│   ├── index.html          # 主页面 (单页应用，含完整 CSS/JS)
│   ├── charts.js           # 今日扫盘渲染引擎
│   ├── live.js             # 临场面板交互脚本
│   ├── plan.html           # 精选计划单页面 (API版)
│   ├── v1~v6.html          # 历史版本存档
│   ├── debug.html          # 调试页面
│   └── test.html           # 测试页面
│
├── data/
│   ├── input/              # pipeline 输入
│   ├── output/             # pipeline 输出
│   ├── processed/          # 已处理的输入
│   ├── locked_data.json    # 核心输入：锁定比赛数据
│   ├── factor_params.json  # 因子参数
│   ├── match_info.json     # 标准化比赛信息 (60场, 06-11~06-25)
│   ├── monte_carlo_result.json  # MC模拟结果
│   ├── ddi_result.json     # DDI校准结果
│   ├── fit_score_result.json    # 贴合度评分
│   ├── rating_result.json       # 最终推荐评级
│   ├── ai_judgment.json    # AI综合判断
│   ├── raw_jczq.json       # 竞彩网原始API数据缓存
│   ├── wc_schedule.json    # 世界杯赛程+赔率数据
│   ├── team_profiles.json   # 48队赛前档案 (FIFA排名/身价/联合会)
│   ├── plan_data.json      # 精选计划单生成数据
│   ├── plan.html           # 今日计划单页面 (gen_plan版)
│   ├── plan_index.html     # 按比赛日导航首页
│   ├── plan_周四.html ~ plan_周三.html  # 各比赛日计划单页面
│   ├── review.json         # 复盘输入
│   ├── review_done.json    # 已完成复盘
│   ├── sofascore_matches.json  # SofaScore缓存
│   └── archive/            # 旧数据归档
│
├── static/
│   ├── bg1.png / bg2.png   # 背景图
│   └── flags/              # 国家队队旗 (48面PNG)
│
├── work/                   # 待处理/迁移脚本
├── _archive/               # 已归档的历史文件
├── 世界杯2.7/            # Rev1.17 完整备份 (2026-06-14)
└── _merge_backup_20260611/ # 合并前的原始备份
`

### 1.3 全数据流 (含计划单 + APP)

`
竞彩网 11:00 开盘
  ↓ fetch_jczq.py + enrich_odds.py
framework.db（SQLite，唯一数据源头）
  ↓
┌─ run_all.py (预测管道) ─────────────────┐
│ λ计算→因子修正→MC模拟→DDI校准→贴合度→评级  │
│          ↓ rating_result.json           │
│          ↓ 方向+评级写入 DB              │
└─────────────────────────────────────────┘
  ↓                             ↓
app.py (5020)              gen_plan.py
  ↓ index.html               ↓ plan_data.json
  ↓ Flask 页面                ↓ plan.html / plan_xxx.html
                              ↓ 精选计划单页面
  ↓
sync → analysis.json → APP 端 (GitHub Pages)
`

---

## 第二章：核心业务流程与推理逻辑

### 2.1 数据采集 (8 大数据板块)

| # | 板块 | 数据源 | 自动化 | 脚本 |
|---|------|--------|--------|------|
| 1 | 比赛基本信息 | 竞彩网 API | 全自动 | fetch_jczq.py |
| 2 | 竞彩赔率 (5彩池) | 竞彩网 API | 全自动 | fetch_jczq.py + enrich_odds.py |
| 3 | 亚洲盘口 | 竞彩网 | 全自动 | enrich_odds.py |
| 4 | 联赛赛季数据 | SofaScore | 半自动 | fetch_sofascore_data.py |
| 5 | 近期战绩 | SofaScore | 半自动 | fetch_form.py |
| 6 | 伤停信息 | — | 需人工 | 标记缺失 |
| 7 | 战意背景 | 手动分析 | 需人工 | 标记缺失 |
| 8 | 特殊事项 | 手动 | 需人工 | 标记缺失 |

#### 竞彩网彩池 (5个)

| 彩池 | 字段 | 说明 |
|------|------|------|
| had | h/d/a | 标准胜平负 SP |
| hhad | h/d/a + goalLine | 让球胜平负 SP + 让球数 |
| ttg | s0~s7 | 总进球数 (0~7+) |
| crs | s{hh}{aa}s{hh}{aa} | 正确比分 |
| hafu | hh/hd/ha/dh/... | 半全场 |

#### 赛果补录

从 500.com (zx.500.com/jczq/kaijiang.php) 获取已完赛比分+半全场：
`powershell
python fetch_results.py --date 2026-06-22
`

### 2.2 推理模型

系统基于泊松分布假设的统计模拟，非机器学习模型。

**核心假设**: 进球数 ~ Poisson(λ)，主客队独立。

#### λ计算

**普通模式**:
`
λ_h = home_xG × away_xGA / 联赛平均xGA
λ_a = away_xG × home_xGA / 联赛平均xGA
`

**世界杯模式** (calc_initial_lambda_wc):
- 应用联合会汇率折算 (CONFED_DEFLATION)
- 支持中立场地
- 使用联合会默认 xGA (CONFED_DEFAULT_XGA)

#### 因子修正

`
λ_h_final = λ_h × (1+injury) × (1+motivation) × (1+altitude)
`

世界杯扩展: INJURY_WORLD_CUP_MAX=0.35, KEY_PLAYER_INJURY_BOOST=0.10, 中立场地处理, GROUP_MATCHDAY_FACTOR, KNOCKOUT_LAMBDA_FACTOR

#### 蒙特卡洛模拟 → DDI校准 → 贴合度评分 → 评级

详见代码: monte_carlo.py / ddi.py / fit_score.py / rating.py

#### 赛前预测模式 (squad_power)

当比赛数据中缺少xG/SofaScore统计数据时（如世界杯开幕前数据冻结阶段），系统自动降级至赛前预测模式：

**数据来源**: data/team_profiles.json (48支参赛队赛前档案)

**核心逻辑**:
`
实力分 = 0.4 × rank_score + 0.6 × value_score
rank_score = 1 - (FIFA排名 - 1) / 49   [归一化至0~1]
value_score = 身价(€M) / 1350M        [归一化至0~1]

λ_h = base_rate × (实力分_h / avg实力分)
λ_a = base_rate × (实力分_a / avg实力分)
`

**联合会惩罚**: 对CONCACAF(0.78)/CAF(0.72)/AFC(0.75)等弱联合会球队实力分乘以CONFED_DEFLATION系数

**触发条件**: run_all.py自动检测：若全部4项xG字段均为None且match为世界杯类型，自动调用squad_power.estimate_match()，无需人工干预。

---

## 第三章：复盘模块

### 3.1 复盘流程

对前一天的预测结果做复盘诊断：
`
python review.py
`

### 3.2 4 层诊断

| 层 | 诊断内容 | 判定 |
|----|---------|------|
| 数据层 | 实际赛果是否为极端值 (4球差或6总进球) | 数据异常/需手动 |
| 物理层 | λ差方向是否与实际方向一致 | 物理方向偏差 |
| 因子层 | 因子方向是否与实际方向相同 | 因子修正偏差 |
| 市场层 | 市场赔率倾向与实际方向的关系 | 市场信号正确/误导 |

### 3.3 核心指标

- 累计命中率：所有预测的 hit 率
- 本轮命中率：当天复盘的预测命中率
- 本轮 vs 累计偏差

### 3.4 数据文件

| 文件 | 说明 |
|------|------|
| data/review.json | 复盘输入（已完赛比分） |
| data/review_done.json | 复盘结果（含诊断） |
| history.csv | 历史预测记录 |

---

## 第四章：管理后台 (Dashboard)

### 4.1 启动方式

`powershell
cd D:\V3.3.3-Core
python dashboard.py
# 浏览器打开 http://127.0.0.1:5021/
`

### 4.2 功能

- 7 个统计卡片（总比赛、已赛果、已预测、命中/未命中、命中率、待预测）
- 搜索和按状态筛选
- 完整比赛数据表格
- APP 数据状态概览

---

## 第五章：精选计划单系统

### 5.1 两套方案

| 方案 | 引擎 | 输出方式 | 适用场景 |
|------|------|---------|---------|
| API版 | plan_engine.py | Flask API → plan.html | 实时在线查看 |
| 独立生成版 | gen_plan.py + gen_plan_html.py | plan_data.json → HTML文件 | 离线/按天批量生成 |

### 5.2 API版 (plan_engine.py)

详见 精选计划单.md。

### 5.3 独立生成版 (gen_plan.py)

#### 使用方式

`powershell
python gen_plan.py                     # 全部比赛
python gen_plan.py --prefix 周日        # 只处理周日开头的比赛
python gen_plan.py --ids 周日037 周日038 # 只处理指定比赛
python gen_plan_html.py                 # 从 plan_data.json 生成页面
`

#### 按天批量生成

`powershell
python run_plans_by_day.py
`

#### 复选打包腿规则

将同一场比赛、同一玩法类型的 2-3 个选项打包成一条虚拟腿：
- 虚拟赔率 = 1 / (1/赔率1 + 1/赔率2 + ...)
- 虚拟概率 = 各选项概率之和
- 评分 = 各选项 score 均值

| 打包类型 | 具体组合 | 适用场景 |
|---------|---------|---------|
| 总进球打包 | 0/1球 ~ 6/7球 (所有相邻对) | 覆盖任意进球区间 |
| 半全场打包 | 主不败(胜胜+平胜)、客不败(负负+平负) | 覆盖不败场景 |
| 比分打包 | 主小胜(1-0+2-0)、客小胜(0-1+0-2) | 覆盖小胜格局 |
| 方向打包(让球盘) | 让胜/平、让平/负 | 覆盖让球盘不败/不胜 |
| 方向打包权重 | 评分 ×1.3 | 提升方向打包在串关中的排位 |

#### 输出文件

| 文件 | 说明 |
|------|------|
| data/plan_data.json | 计划单生成数据 |
| data/plan.html | 今日计划单页面 |
| data/plan_周一.html ~ plan_周日.html | 各比赛日计划单 |
| data/plan_index.html | 按天导航首页 |
| data/plan_20260611.html ~ plan_20260622.html | 按日期计划单 |

---

## 第六章：APP 数据同步

### 6.1 同步流程

`python
# 参考 D:\V3.3.3-Core\work\_apply_update.py
# 从 framework.db + match_info.json → analysis.json
# 输出到 C:\Users\gjj\Documents\Codex\2026-06-14\app-app\outputs\data\
`

### 6.2 analysis.json 结构

`json
{
  "rating": [{ "id": "周日037", "home": "...", "away": "...", 
               "direction": "胜", "rating": "A", "fit_score": 8.5,
               "actual_score": "2-1", "hit": true, ... }],
  "AL": { "06-21": ["周日037", "周日038", ...], ... },
  "mc": [ ... ],
  "narratives": { ... }
}
`

### 6.3 卡片三种状态

| 状态 | 判断 | 显示 |
|------|------|------|
| 已完成 | actual_score 有值 | 赛果+方向+命中 |
| 今日预测 | direction 有值，actual_score 空 | 方向+评分 |
| 等待预测数据 | direction 空 | "等待预测数据" |

---

## 第七章：前端页面设计

### 7.1 设计规范

深色科技风，CSS变量体系，布局: 固定侧边栏(250px) + 自适应内容区

### 7.2 Web版页面 (Flask, 5020)

| 路由 | 说明 |
|------|------|
| / | 主页面（今日扫盘） |
| /api/latest | 今日比赛数据 API |
| /api/analyze | 全分析管道 API |
| /api/review | 复盘提交 API |
| /api/history/* | 历史运行/比赛 API |
| /api/plan/<level> | 精选计划单 JSON |
| /plan/<level> | 精选计划单页面 |
| /api/live/* | 临场信息/重校 |
| /debug | 调试页面 |

### 7.3 APP版页面 (GitHub Pages)

| 页面 | 功能 |
|------|------|
| index.html | 按销售日期分组的比赛卡片 |
| 积分榜 | 小组积分排名 |

---

## 第八章：数据库设计

### 表: runs

| 字段 | 说明 |
|------|------|
| id | 自增主键 |
| date / prediction_date | 运行/预测日期 |
| factor_params | JSON参数 |
| run_type | live/backtest |
| total_matches / hit_count / avg_fit_score | 统计 |

### 表: matches (50+字段)

核心: match_id, home/away, event/time, lambda系列, 概率系列, DDI系列, fit_score, rating, direction, top2/3预测, 复盘字段

**当前规模**: 428条记录 (截至2026-06-23)，覆盖 06-11 至 06-25 共 15 个销售日期

---

## 附录A：核心数学公式

| 公式 | 位置 |
|------|------|
| λ_h = xG_h × xGA_a / league_avg_xGA | lambda_calc.py |
| λ_h_final = λ_h × (1+injury) × (1+motivation) × (1+altitude) | run_all.py |
| P(home_win) = count(Poisson(λ_h) > Poisson(λ_a)) / N | monte_carlo.py |
| DDI = P_physical - P_market | ddi.py |
| fit_score = (Σ7维 - s7) × predictability × (10/11) | fit_score.py |
| score = model_prob × (fit/10) × (1+edge) | plan_engine.py |

---

## 附录B：更新日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-06-23 | Rev1.18 | 新增 gen_plan.py / gen_plan_html.py / run_plans_by_day.py 独立计划单生成系统 |
| 2026-06-23 | Rev1.18 | 新增 dashboard.py 管理后台 (5021端口)，7统计卡片+搜索筛选 |
| 2026-06-23 | Rev1.18 | 新增 fetch_results.py 赛果补录 (500.com 数据源) |
| 2026-06-23 | Rev1.18 | 新增 review.py 复盘模块，4层诊断 (数据/物理/因子/市场) |
| 2026-06-23 | Rev1.18 | 新增 plan_index.html + 按比赛日/日期两种 plan 导航体系 |
| 2026-06-23 | Rev1.18 | 复选打包腿规则：相邻TTG/半全场不败/比分主客小胜/方向权重×1.3 |
| 2026-06-23 | Rev1.18 | 赛果数据源改为 500.com (zx.500.com/jczq/kaijiang.php) |
| 2026-06-23 | Rev1.18 | APP同步修复：mc数据从monte_carlo_result.json重新加载 |
| 2026-06-23 | Rev1.18 | 比赛规模扩大至60场，15个销售日 (06-11~06-25) |
| 2026-06-23 | Rev1.18 | 8大数据板块文档化，标记自动化程度 |
| 2026-06-14 | Rev1.17 | 修复 isFutureDate 硬编码(改为>6月15)，仅显示今明两天可预测，16号后仍显示"等待预测数据" |
| 2026-06-14 | Rev1.17 | 修复 displayDir "让让负" bug (dir已含"让"时不再重复加) |
| 2026-06-14 | Rev1.17 | 历史数据修正：周日009-012方向/评级校正 |
| 2026-06-14 | Rev1.17 | 完整备份至 D:\世界杯2.7 |
